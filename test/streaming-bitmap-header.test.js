const assert = require('assert');
const constants = require('../lib/bitmap-constants');
const StreamingBitmapHeader = require('../lib/streaming-bitmap-header');
const { join } = require('path');
const { readFile } = require('fs').promises;


describe('streaming-bitmap-header', () => {

    const actualFile = '../lib/test-bitmap.bmp';

    it.only('getBitmapHeader', () => {

    });
});